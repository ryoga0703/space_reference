<div class="admin-tastes">
  <div class="container">
    <div class="row py-5">
      <h3 class="col col-md-2 py-1 text-center offset-md-1" style="background-color:rgba(67,224,0,0.4);">
        <strong>会員一覧</strong>
      </h3>
    </div>

    <div class="row mt-4">
      <div class="col-md-6 offset-md-6 d-flex align-items-center justify-content-center text-danger">
        ※名前が詳細ページのリンクになっております。
      </div>
    </div>

    <div class="row">
      <div class="col mx-auto">
        <table class="table table-bordered">
          <thead style="background-color:rgba(67,224,0,0.4);">
            <tr>
              <th>会員ID</th>
              <th>名前</th>
              <th>メールアドレス</th>
              <th>ステータス</th>
            </tr>
          </thead>
          <tbody style="background-color:rgba(248,255,228,0.8);">
            <% @customers.each do |customer| %>
              <tr>
                <td><%= customer.id %></td>
                <td>
                  <%= link_to customer_path(customer.id), style: "color: #000; text-decoration: none;" do %>
                    <%= customer.last_name %><%= customer.first_name %>
                  <% end %>
                </td>
                <td><%= customer.email %></td>
                <% if customer.is_deleted == false %>
                  <td class="text-success font-weight-bold">有効</td>
                <% else %>
                  <td class="text-secondary font-weight-bold">退会</td>
                <% end %>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>